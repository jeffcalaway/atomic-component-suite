const setup        = require('./taxonomy/setup');
const taxInterface = require('./taxonomy/interface');
const functions    = require('./taxonomy/functions');

module.exports = {
  setup,
  taxInterface,
  functions
}